const mainEl = document.getElementById("root");
const imgDiceBtn = document.querySelector(".img-dice-btn");

const timer = [10000];

const apiUrl = "https://api.adviceslip.com/advice";

export { mainEl, imgDiceBtn, timer, apiUrl };
